"""
OmicsCRISPR — Phase 1 Multi-Omics Data Pipeline

Modules:
  config          — paths, URLs, cell type definitions
  download_depmap — DepMap Avana CRISPR screen data
  download_encode — ENCODE RNA-seq + ATAC-seq
  build_splice_db — GENCODE v44 splice site database
  run_pipeline    — master runner + status reporting
"""
